# To Install Ubuntu In Termux Copy this in Termux:
apt update && apt install git -y && git clone https://github.com/nicolasgit-11/Install-Ubuntu-in-Termux && cd Install-Ubuntu-in-Termux && ./install-ubuntu.sh
